#include "users.h"
#include "ui_users.h"
//#include <QStandardItemModel>
/*users::users(QWidget *parent) :
    QDialog(parent),
    ui(new Ui::users)
{
    ui->setupUi(this);
}

users::~users()
{
    delete ui;
}

void users::on_pushButton_clicked()
{
ui->tableWidget->insertRow(ui->tableWidget->rowCount());
ui->tableWidget->setItem(ui->tableWidget->rowCount()-1,0,new QTableWidgetItem("Paulo"));
ui->tableWidget->setItem(ui->tableWidget->rowCount()-1,1,new QTableWidgetItem("Pizarro"));
ui->tableWidget->setItem(ui->tableWidget->rowCount()-1,2,new QTableWidgetItem("paulo123@gmail.com"));;
}

void users::on_Mostrar_clicked()
{ui->tableWidget->setColumnCount(3);
    QStringList l;
    l<<"Nombre"<<"Apellido"<<"Correo";
    ui->tableWidget->setHorizontalHeaderLabels(l);
    ui->tableWidget->setColumnWidth(0,120);
    ui->tableWidget->setColumnWidth(1,120);
    ui->tableWidget->setColumnWidth(2,200);

}

void users::on_MessageButton_clicked()
{
    this->close();
}*/
